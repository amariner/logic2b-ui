---
title: Hover Card
description: For sighted users to preview content available behind a link.
---

import ComponentPreview from "@/components/ComponentPreview.astro";

<ComponentPreview name="hover-card-demo" />

## Installation

```bash
npx shadcn@latest add https://ui.logic2b.dev/r/hover-card.json
```

## Usage

```tsx
import {
  HoverCard,
  HoverCardContent,
  HoverCardTrigger,
} from "@/components/ui/hover-card"

<HoverCard>
  <HoverCardTrigger>@logic2b</HoverCardTrigger>
  <HoverCardContent>
    The React framework for building shadcn-compatible UIs.
  </HoverCardContent>
</HoverCard>
```

## API

| Prop | Type | Default |
| --- | --- | --- |
| `align` (HoverCardContent) | `start \| center \| end` | `center` |
| `sideOffset` (HoverCardContent) | `number` | `4` |
| `openDelay` / `closeDelay` (HoverCard) | `number` (ms) | `700` / `300` |
