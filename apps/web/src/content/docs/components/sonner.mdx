---
title: Sonner
description: An opinionated toast component.
---

import ComponentPreview from "@/components/ComponentPreview.astro";
import Install from "@/components/Install.astro";

<ComponentPreview name="sonner-demo" />

## Installation

<Install name="sonner" />

## Usage

Add the `Toaster` component once, then call `toast()` from anywhere.

```tsx
import { Toaster } from "@/components/ui/sonner"

<Toaster />
```

```tsx
import { toast } from "sonner"

toast("Event has been created")
```

## Usage notes

The `Toaster` component wraps sonner's `Toaster` and forwards every
[`ToasterProps`](https://sonner.emilkowal.ski/toaster). Toasts themselves are
created imperatively with `toast()`.

## Examples

### With an action

<ComponentPreview name="sonner-action-demo" />

### Promise

<ComponentPreview name="sonner-promise-demo" />
