---
title: Introduction
description: Build interfaces fast, assisted by AI. Copy-paste React components you own.
---

logic2b ui is a collection of beautifully designed, accessible React components
that you copy into your project. **This is not a component library you install
from npm — it's code you own.**

## Principles

- **Open Code.** Every component ships as source code. Read it, change it, own it.
- **Composable.** Built on [Radix UI](https://www.radix-ui.com) primitives and
  Tailwind CSS, with a small, predictable API surface.
- **AI-Ready.** The whole registry and documentation are consumable by LLMs:
  plain-JSON registry at [/r/index.json](/r/index.json), Markdown versions of
  every page, and [/llms.txt](/llms.txt).
- **Edge-first.** The site and registry are fully static, served from
  Cloudflare's edge.

## Install with one command

Every component is available from the logic2b registry through the CLI — it
copies the source into your project and resolves dependencies for you:

```bash
npx logic2b@latest add button
```

The registry uses an open, widely-adopted JSON format, so third-party tools and
AI agents can consume it too.

## Where to go next

- [Installation](/docs/installation) — set up your project.
- [Theming](/docs/theming) — CSS variables, dark mode, radius.
- [Cross-platform tokens](/docs/cross-platform-tokens) — one preset exported to
  Figma Variables, portable JSON, CSS, iOS and Android.
- [VS Code extension](/docs/vscode) — browse the registry, install source and
  apply presets from a local or remote workspace.
- [3D extras](/docs/3d-extras) — optional, token-aware hero scenes and product
  viewers without adding Three.js to the base registry.
- [Components](/docs/components) — browse the catalog.
- [Live starter demos](/demos) — run the complete marketing, dashboard and
  authentication compositions generated by CLI and MCP.
- [For LLMs & Agents](/docs/llms) — how to consume logic2b from an AI agent.
- [Compare integration paths](/docs/integration-paths) — choose between the
  website, CLI, remote/local MCP and raw registry contracts.
