---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { CreateStudio } from "@/components/create/CreateStudio";
---

<BaseLayout
  title="Create — logic2b/ui"
  description="Design your theme live: base color, accent, charts, radius and fonts. Copy the CSS, generate components.json, or scaffold a new project with the CLI."
  cta="get-code"
>
  <CreateStudio client:load />
</BaseLayout>
