import type { RegistryAccessibility } from "./types.ts"

const key = (keys: string[], action: string) => ({ keys, action })
const contract = (
  support: RegistryAccessibility["support"],
  pattern: string,
  primitive: string | undefined,
  keyboard: RegistryAccessibility["keyboard"],
  aria: string[],
  consumer: string[],
  limitations?: string[],
): RegistryAccessibility => ({
  support,
  pattern,
  ...(primitive ? { primitive } : {}),
  keyboard,
  aria,
  consumer,
  ...(limitations?.length ? { limitations } : {}),
})

const activate = key(["Enter", "Space"], "Activate the focused control.")
const dismiss = key(["Escape"], "Close the open surface and restore focus.")
const arrows = key(["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight"], "Move within the composite control.")

export const ACCESSIBILITY_CONTRACTS: Record<string, RegistryAccessibility> = {
  button: contract("native", "button", "HTMLButtonElement", [activate], ["Uses native button semantics unless asChild changes the rendered element."], ["Keep a visible label or provide aria-label for icon-only actions."]),
  card: contract("consumer", "content group", "HTML div", [], ["Does not impose a role; headings, descriptions and actions retain their native semantics."], ["Use a heading for the title and add a labelled region only when the card needs to be a navigation landmark."]),
  input: contract("native", "textbox", "HTMLInputElement", [], ["Forwards native input semantics, required, disabled and aria-invalid state."], ["Associate a visible label with id/htmlFor or provide aria-label."]),
  label: contract("native", "form label", "HTMLLabelElement", [], ["Uses native label semantics and forwards htmlFor to its labelled control."], ["Point htmlFor at exactly one labelable control and keep the label text visible."]),
  badge: contract("consumer", "status text", "HTML span", [], ["Does not add a live-region role; its text remains available in the normal reading order."], ["Do not communicate state by color alone; add role=status only when dynamically updated status must be announced."]),
  item: contract("consumer", "content item", "HTML div", [], ["Preserves the semantics of nested headings, links and buttons without imposing a role."], ["Choose list/listitem, article or link semantics at the composition boundary when the surrounding collection requires them."]),
  avatar: contract("primitive", "image with fallback", "Radix Avatar", [], ["The image forwards its accessible name; the fallback text remains available when the image cannot load."], ["Provide useful alt text for informative avatars and empty alt text when the nearby name is redundant."]),
  skeleton: contract("consumer", "loading placeholder", "HTML div", [], ["Is hidden from assistive technology by default and does not announce loading on its own."], ["Put the containing region in an appropriate busy/status state and preserve a stable accessible name while content loads."], ["A standalone skeleton has no semantic loading announcement."]),
  textarea: contract("native", "textbox", "HTMLTextAreaElement", [], ["Forwards native multiline textbox semantics and aria-invalid state."], ["Associate a visible label and expose validation messages with aria-describedby."]),
  accordion: contract("primitive", "accordion", "Radix Accordion", [activate, key(["ArrowUp", "ArrowDown", "Home", "End"], "Move between accordion triggers.")], ["Triggers expose aria-expanded and aria-controls; panels are linked to their headings."], ["Use concise trigger text and preserve the heading hierarchy."]),
  collapsible: contract("primitive", "disclosure", "Radix Collapsible", [activate], ["The trigger manages aria-expanded and aria-controls for its content."], ["Use a button-like trigger with text that describes the hidden content."]),
  dialog: contract("primitive", "modal dialog", "Radix Dialog", [dismiss, key(["Tab", "Shift+Tab"], "Cycle focus inside the modal surface.")], ["Content is a dialog, traps focus and restores it to the trigger on close.", "Title and description wire aria-labelledby and aria-describedby."], ["Always render DialogTitle; add DialogDescription when explanatory copy exists."]),
  "alert-dialog": contract("primitive", "alertdialog", "Radix Alert Dialog", [dismiss, key(["Tab", "Shift+Tab"], "Move between confirmation actions.")], ["Uses alertdialog semantics, traps focus and requires an explicit action."], ["Describe the consequence and give destructive/escape actions unambiguous labels."]),
  "dropdown-menu": contract("primitive", "menu", "Radix Dropdown Menu", [activate, arrows, key(["Home", "End"], "Move to the first or last item."), dismiss], ["Manages menu/menuitem roles, roving focus and checked state for checkbox/radio items."], ["Provide an accessible name for the trigger and use menu items only for actions/options."]),
  popover: contract("primitive", "non-modal dialog", "Radix Popover", [activate, dismiss], ["The trigger manages aria-expanded and aria-controls; focus can move into the portal content."], ["Label the trigger and provide an explicit close path for complex content."]),
  tooltip: contract("primitive", "tooltip", "Radix Tooltip", [key(["Tab"], "Opening trigger focus reveals the tooltip."), dismiss], ["Tooltip content is associated with its trigger and opens for keyboard focus as well as hover."], ["Tooltips supplement rather than replace the trigger's accessible name."]),
  sheet: contract("primitive", "modal dialog", "Radix Dialog", [dismiss, key(["Tab", "Shift+Tab"], "Cycle focus inside the sheet.")], ["Uses dialog semantics with focus trapping/restoration and title/description linkage."], ["Render SheetTitle and provide a labelled close control."]),
  tabs: contract("primitive", "tabs", "Radix Tabs", [key(["ArrowLeft", "ArrowRight", "Home", "End"], "Move focus and selection between tabs according to orientation."), key(["Tab"], "Move from the active tab into its tabpanel.")], ["Manages tablist, tab and tabpanel roles plus aria-selected/controls linkage."], ["Give every trigger a concise label and keep each panel associated with one trigger."]),
  "hover-card": contract("primitive", "hover preview", "Radix Hover Card", [key(["Tab"], "Focusing the trigger reveals the preview."), dismiss], ["The preview opens for focus as well as pointer hover."], ["Keep the linked destination usable without the preview; do not put required actions only inside it."]),
  "context-menu": contract("primitive", "context menu", "Radix Context Menu", [key(["Shift+F10", "ContextMenu"], "Open the context menu from the keyboard."), arrows, dismiss], ["Manages menu roles, roving focus and checked state."], ["Ensure every pointer-only context action is available through another keyboard-reachable path."]),
  menubar: contract("primitive", "menubar", "Radix Menubar", [arrows, activate, dismiss], ["Manages menubar/menu/menuitem roles and roving focus across top-level menus."], ["Use it for application commands, not site navigation, and label icon-only items."]),
  "navigation-menu": contract("primitive", "navigation", "Radix Navigation Menu", [activate, arrows, dismiss], ["Keyboard-focusable triggers manage expanded content; active links can expose aria-current."], ["Wrap the component in a labelled nav landmark when multiple navigation regions exist."]),
  drawer: contract("primitive", "modal dialog", "Vaul Dialog", [dismiss, key(["Tab", "Shift+Tab"], "Cycle focus inside the drawer.")], ["Uses dialog title/description primitives and modal focus management."], ["Provide DrawerTitle, an accessible close control and a non-drag dismissal path."]),
  form: contract("authored", "form validation", "React Hook Form + Radix Label", [], ["FormControl derives id, aria-invalid and aria-describedby from field state.", "Descriptions and error messages receive stable ids."], ["Render FormLabel and useful error text; move focus or provide a summary after failed submission."]),
  checkbox: contract("primitive", "checkbox", "Radix Checkbox", [activate], ["Exposes role=checkbox and aria-checked, including the mixed state."], ["Associate the control with a visible Label or aria-label."]),
  "radio-group": contract("primitive", "radio group", "Radix Radio Group", [arrows, key(["Space"], "Select the focused option.")], ["Manages radiogroup/radio roles, aria-checked and roving tab focus."], ["Give the group an accessible name and label every option."]),
  switch: contract("primitive", "switch", "Radix Switch", [activate], ["Exposes role=switch and aria-checked."], ["Associate it with a label that describes the setting, not only the current state."]),
  select: contract("primitive", "listbox select", "Radix Select", [activate, arrows, key(["Home", "End"], "Move to the first or last option."), dismiss], ["Manages combobox/listbox/option semantics, selected state and trigger-value linkage."], ["Provide a visible label or aria-label for the trigger."]),
  "native-select": contract("native", "select", "HTMLSelectElement", [arrows, key(["Alt+ArrowDown"], "Open the platform option picker where supported.")], ["Uses native select/listbox semantics and forwards invalid/disabled state."], ["Associate it with a label; do not rely on placeholder-like first options as the name."]),
  slider: contract("primitive", "slider", "Radix Slider", [arrows, key(["Home", "End", "PageUp", "PageDown"], "Move the focused thumb by the supported increment." )], ["Each thumb exposes slider value/min/max semantics and supports range sliders."], ["Provide a contextual aria-label or aria-labelledby for every thumb."]),
  progress: contract("primitive", "progressbar", "Radix Progress", [], ["Exposes progressbar semantics and a default accessible name."], ["Replace the default label with task-specific context; omit value only for indeterminate progress."]),
  toggle: contract("primitive", "toggle button", "Radix Toggle", [activate], ["Exposes pressed state through aria-pressed."], ["Icon-only toggles require an aria-label describing the action."]),
  "toggle-group": contract("primitive", "toggle group", "Radix Toggle Group", [arrows, activate], ["Uses roving focus and pressed/selected state for single or multiple selection."], ["Label the group and every icon-only item."]),
  "button-group": contract("consumer", "group of actions", "HTML div", [], ["Groups native buttons visually without replacing their button semantics."], ["Add role=group and an accessible name when the buttons form one conceptual control."]),
  "input-group": contract("consumer", "composite input", undefined, [], ["The inner native input/textarea keeps its semantics; decorative addons can be hidden from assistive technology."], ["Label the inner control and give addon buttons accessible names."]),
  field: contract("authored", "labelled form field", "HTML fieldset/label", [], ["Field labels, descriptions and validation messages can be associated with the nested native control."], ["Connect help and error text with aria-describedby and set aria-invalid only while the value is invalid."]),
  "tags-input": contract("authored", "multi-value textbox", "HTMLInputElement", [key(["Enter", ","], "Commit the current text as a tag."), key(["Backspace"], "Remove the last tag when the text field is empty.")], ["The editable input receives id/aria-label/aria-labelledby; removal buttons name their tag."], ["Provide a visible label and announce additions/removals in application context when that feedback is important."]),
  rating: contract("authored", "radio group", undefined, [arrows, key(["Home", "End"], "Move to the minimum or maximum rating."), key(["Space"], "Select the focused rating.")], ["Interactive stars expose radiogroup/radio and aria-checked; read-only mode is a labelled image."], ["Provide a contextual label and visible text for the meaning of the scale."]),
  "number-field": contract("native", "spinbutton", "HTMLInputElement type=number", [arrows, key(["Enter"], "Commit typed input according to native number-field behavior.")], ["The input retains native spinbutton semantics; step buttons are native buttons with labels."], ["Associate the input with a label and communicate min/max constraints in visible help text."]),
  autocomplete: contract("authored", "editable combobox", undefined, [key(["ArrowUp", "ArrowDown"], "Move the active suggestion."), key(["Enter"], "Choose the active suggestion."), dismiss], ["Uses combobox/listbox/option semantics with aria-expanded, aria-controls and aria-activedescendant."], ["Provide a label and ensure arbitrary typed text is valid for the use case."]),
  "file-dropzone": contract("authored", "file upload", "HTMLInputElement type=file", [activate], ["A hidden native file input owns the picker; the visible drop surface is keyboard activatable."], ["Provide contextual instructions, accepted formats and validation feedback."]),
  "color-picker": contract("authored", "color input", undefined, [], ["The hexadecimal text input has an accessible name and provides a keyboard-editable fallback."], ["Add a visible label and communicate validation for invalid hex values."], ["The saturation/value area and hue rail are pointer-only; keyboard users must edit the hex field."]),
  pagination: contract("consumer", "pagination navigation", "HTML nav/anchor/button", [activate], ["The root is a navigation landmark and the active page can expose aria-current=page."], ["Give the nav an aria-label and accessible labels to previous/next/icon-only links."]),
  "scroll-area": contract("primitive", "scrollable region", "Radix Scroll Area", [key(["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight", "PageUp", "PageDown"], "Scroll a focused scroll container with the keyboard.")], ["Preserves the semantics of its children while providing custom visual scrollbars."], ["Make the viewport focusable and label it only when the region itself needs keyboard scrolling or landmark semantics."]),
  "aspect-ratio": contract("consumer", "layout wrapper", "Radix Aspect Ratio", [], ["Adds no semantic role and preserves the accessibility tree of its child content."], ["Give contained images, videos and embeds their own accessible names or captions."]),
  command: contract("primitive", "combobox with listbox", "cmdk", [key(["ArrowUp", "ArrowDown"], "Move through matching commands."), key(["Enter"], "Run the active command."), dismiss], ["The input and list coordinate with combobox/listbox semantics and active descendant state."], ["Give the dialog and input useful labels; commands must remain understandable without shortcut text."]),
  sonner: contract("authored", "toast status messages", "Sonner", [dismiss], ["Toast messages use status/alert semantics appropriate to their urgency and expose dismiss actions when configured."], ["Keep messages concise, avoid moving focus automatically and provide a persistent recovery path for consequential actions."]),
  sidebar: contract("authored", "application navigation", undefined, [activate, key(["Tab", "Shift+Tab"], "Move through sidebar controls and links.")], ["Trigger buttons name their action; mobile mode uses a labelled sheet/dialog surface."], ["Wrap navigation links in a labelled nav landmark and mark the current route with aria-current."]),
  kbd: contract("consumer", "keyboard input text", "HTML kbd", [], ["Uses the native kbd element so shortcut text is conveyed as keyboard input."], ["Describe the associated command in nearby text and do not make the shortcut the only way to perform it."]),
  calendar: contract("primitive", "date grid", "React DayPicker", [arrows, key(["PageUp", "PageDown"], "Move between months."), key(["Home", "End"], "Move within the current week."), key(["Enter", "Space"], "Select the focused date.")], ["Uses grid/gridcell semantics with labelled navigation and selected/disabled date state."], ["Provide a label describing the date purpose and expose format/validation guidance near related inputs."]),
  carousel: contract("authored", "carousel", "Embla Carousel", [key(["ArrowLeft", "ArrowRight"], "Move to the previous or next slide."), key(["Tab"], "Reach the explicit previous/next buttons.")], ["The root is a labelled carousel region; slides are labelled groups and controls expose names/disabled state."], ["Give the carousel a contextual aria-label and avoid auto-rotation without a pause control."]),
  resizable: contract("primitive", "separator", "React Resizable Panels", [arrows, key(["Home", "End"], "Move the separator to its supported extent.")], ["Handles expose separator semantics, orientation and value information."], ["Give handles an accessible label when adjacent panel context is ambiguous."]),
  "input-otp": contract("primitive", "one-time-code textbox", "input-otp", [key(["Backspace"], "Clear the current slot and move backward."), key(["ArrowLeft", "ArrowRight"], "Move between code positions where supported.")], ["A single input owns the value while visual slots mirror caret/invalid state."], ["Provide a label, autocomplete=one-time-code where appropriate and never rely only on SMS access."]),
  "tree-view": contract("authored", "tree", undefined, [arrows, key(["Home", "End"], "Move to the first or last visible node."), key(["Enter", "Space"], "Select or activate the focused node.")], ["Uses tree/treeitem/group roles with aria-expanded, aria-selected, level and set position metadata."], ["Give the tree a label and ensure node actions are distinguishable from expand/collapse."]),
  stepper: contract("authored", "step list", "HTML ordered list + buttons", [activate], ["The active item exposes aria-current=step; separators are aria-hidden."], ["Give every trigger descriptive text and do not rely on color alone for completed/current state."]),
  "code-block": contract("native", "code region with copy action", "HTML pre/code/button", [activate], ["Code uses pre/code semantics and the copy button updates its accessible label after success."], ["Provide a language label when it helps comprehension and surface copy failures in application context if critical."]),
  alert: contract("native", "alert", "HTML role=alert", [], ["Uses role=alert so newly mounted urgent messages are announced."], ["Reserve it for important dynamic messages; avoid mounting routine static content as an alert."]),
  spinner: contract("authored", "status", "SVG role=status", [], ["Exposes role=status with a default Loading accessible name."], ["Replace the label when the loading operation needs more context."]),
  empty: contract("consumer", "empty state", "HTML div", [], ["Preserves native heading, paragraph and action semantics without forcing a live-region announcement."], ["Use a heading that names the empty state and make the primary recovery action keyboard reachable."]),
  breadcrumb: contract("native", "breadcrumb navigation", "HTML nav/ol/li", [], ["Uses navigation/list semantics, aria-current=page for the current item and hides decorative separators."], ["Give the nav an aria-label when the page contains multiple navigation landmarks."]),
  separator: contract("primitive", "separator", "Radix Separator", [], ["Can be decorative (hidden from the accessibility tree) or expose separator orientation."], ["Keep decorative=true unless the separator conveys a meaningful structural boundary."]),
  table: contract("native", "data table", "HTML table elements", [], ["Uses native table, row, header and cell elements."], ["Add a caption, scope headers correctly and provide a non-visual name for interactive controls in cells."]),
  timeline: contract("consumer", "chronological list", "HTML div", [], ["Does not impose list semantics, allowing each entry to retain its native content structure."], ["Compose the root and entries as an ordered list when order is meaningful, and expose dates as machine-readable time elements."]),
  motion: contract("consumer", "animated semantic wrapper", "Motion for React", [], ["Preserves the semantics and focus behavior of the rendered child element."], ["Respect prefers-reduced-motion and avoid animating focus, reading order or critical status changes."], ["Motion safety depends on the selected transition and consumer composition."]),
  "motion-fade": contract("consumer", "fade animation", "Motion for React", [], ["Preserves child semantics and accepts reduced-motion behavior from the motion foundation."], ["Keep content available without animation and avoid fading essential feedback too quickly."], ["Motion safety depends on the selected transition and consumer composition."]),
  "motion-slide": contract("consumer", "slide animation", "Motion for React", [], ["Preserves child semantics and accepts reduced-motion behavior from the motion foundation."], ["Use a reduced or instant alternative and do not use position changes to imply a different reading order."], ["Large translations can be uncomfortable unless reduced-motion preferences are honored."]),
  "motion-scale": contract("consumer", "scale animation", "Motion for React", [], ["Preserves child semantics and accepts reduced-motion behavior from the motion foundation."], ["Keep interactive hit targets stable and offer an instant reduced-motion alternative."], ["Large or rapid scale changes can be uncomfortable unless reduced-motion preferences are honored."]),
  "motion-blur": contract("consumer", "blur animation", "Motion for React", [], ["Preserves child semantics and accepts reduced-motion behavior from the motion foundation."], ["Ensure final text is sharp, contrast remains sufficient and reduced-motion users receive an instant transition."], ["Blurred text can be temporarily unreadable during animation."]),
  "scroll-reveal": contract("consumer", "scroll-triggered reveal", "Intersection Observer + Motion", [], ["Content remains in the document reading order and preserves the rendered child semantics."], ["Render meaningful content without requiring scroll activation and disable movement for reduced-motion preferences."], ["Incorrect initial styles can hide content when scripting or observation is unavailable."]),
  parallax: contract("consumer", "decorative parallax", "Motion scroll transforms", [], ["Preserves the semantics of the rendered layer; decorative layers can be hidden from assistive technology."], ["Use empty alt/aria-hidden for decorative imagery, retain a static layout and disable the transform for reduced-motion users."], ["Parallax movement can cause vestibular discomfort unless reduced-motion preferences are honored."]),
  chart: contract("consumer", "data visualization", "Recharts", [], ["Chart containers can expose a labelled figure while tooltip content is available to pointer and keyboard interactions supported by the composed chart."], ["Provide a concise chart name, units and a textual or tabular equivalent for the underlying data."], ["Canvas/SVG marks alone are not a complete accessible data representation."]),
}

export const ACCESSIBILITY_REQUIRED = Object.keys(ACCESSIBILITY_CONTRACTS).sort()

export function accessibilityFor(name: string): RegistryAccessibility | undefined {
  return ACCESSIBILITY_CONTRACTS[name]
}
