# Changelog

## 0.1.0

- Browse components, blocks and charts from the public logic2b registry.
- Install one or many items through the public CLI contract.
- Initialize a workspace and apply `/create` presets to existing projects.
